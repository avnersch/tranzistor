{
  "rewrites": [
    { "source": "/api/now-playing", "destination": "/api/now-playing" },
    { "source": "/api/shazam", "destination": "/api/shazam" },
    { "source": "/api/cron/update-now-playing", "destination": "/api/cron/update-now-playing" }
  ],
  "functions": {
    "api/shazam.js": {
      "memory": 1024,
      "maxDuration": 60
    },
    "api/cron/update-now-playing.js": {
      "maxDuration": 30
    }
  }
}
